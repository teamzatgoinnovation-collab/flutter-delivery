name: delivery
description: ZatGo Delivery — last-mile driver client (zatgo_core).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^15.1.2
  http: ^1.6.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  geolocator: ^14.0.2
  url_launcher: ^6.3.1
  shared_preferences: ^2.5.3
  permission_handler: ^12.0.1
  flutter_local_notifications: ^19.4.0
  zatgo_dart_sdk:
    path: ../../../SharedSDK/dart_sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
